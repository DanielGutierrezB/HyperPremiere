{
  "name": "hyperpremiere-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Motor Node in-process del panel CEP de HyperPremiere (providers + prompt + render)",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "hyperframes": "^0.7.28"
  }
}
